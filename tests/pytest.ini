[pytest]
addopts = -s -v
markers =
    sanity: marks tests as sanity
    sonic: mark tests with sonic which need sonic dut
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
    ignore::DeprecationWarning
