[pytest]
addopts = -s -v
markers =
    sanity: marks tests as sanity
    dut: mark tests with dut which need dut
    skip_if_athena: marks the tests as athena not supported
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
    ignore::DeprecationWarning
